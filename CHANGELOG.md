# Changelog

## 0.3.4 — 2026-09-19

- 修复 reference 中以 `scripts/` 等资源目录开头的代码路径被错误按 reference 子目录解析的问题，并添加回归覆盖。
- 将 `disclose_reference` 归入职责边界评分，避免把资料泄露误扣为 token 或模块化问题；补充语义审查的目标契约、来源说明和路径误报校准。
- 发布入口随项目同步完整八维审查参考文件，避免入口链接失效。

## 0.3.3 — 2026-09-19

- 对话入口在“检测、体检、审查”等范围不明确的请求中，先让用户选择快速静态审查或完整八维审查；明确范围的请求直接执行。
- 快速模式交付静态暂评分并保留职责边界、异常处理待语义审查；完整模式不在语义导入完成前交付静态评分结论。
- 增加入口范围选择的回归覆盖；CLI 的显式 `--static` / `--semantic` 语义和完整审查门禁保持不变。
- 验证：49 项测试、严格类型检查、构建和编译后 HTML 静态审查通过；更新宿主对照场景的来源锚点以匹配新入口契约。

## 0.3.2 — 2026-09-19

- 将公开 Skill 入口、文档、评测描述和维护约定改为宿主无关的通用流程；不再包含本机绝对路径、特定 Agent 产品或固定技能目录。
- 默认解析忽略 `.pnpm-store`，项目或 monorepo 审查不会把包管理器缓存当成 Skill 内容；增加该场景和公开入口可移植性的回归测试。
- 历史宿主试验保留可复现实验条件与结论，但移除带本机路径的原始运行产物，避免将环境快照作为公开基线。
- 验证：48 项测试、严格类型检查、构建、编译后 HTML/JSON 静态审查通过；部署 182 个文件并核对哈希。

## 0.3.1 — 2026-09-18

- 完整体检改为可验证完成状态：普通 `audit` 未指定模式时保留静态报告并退出 3；`--static` 表示明确接受静态范围。
- Skill 默认直接完成宿主或已配置模型的语义审查；八维均 assessed 且有数值分才算完整。
- 已配置、已启用或用户已选择的模型服务不重复询问；只有新增或切换到未知远程地址时才说明发送目标与范围，显式离线要求始终优先。

## 0.3.0 — 2026-09-17

- 验证：45 项测试、类型检查、构建和编译后 CLI 任务包/语义导入/三种报告/比较通过。

- 恢复八维百分制设计分：四项标准达成率与缺陷上限取较低值，达标/不足均留逐字证据；静态暂评与行为结果分开。
- 排除 Python 缓存；识别 tests 之外的自检/扫描候选，缺少测试资源仅提示。
- 增加 prepare-review 和 --semantic-review，宿主语义结论经指纹、覆盖清单与原文校验进入统一报告。
- 报告协议 1.2，旧政策报告仍可读取，跨政策不产生分数改善结论。

## 2026-09-17 — 宿主 Agent 对照评测

- 参考 skill-creator 加入同题有 / 无 Skill 的宿主执行流程，复用现有案例协议和 Grader；独立目录保留输入、输出与执行标识。
- 固定离线审查、旧高分解读、跨口径比较三个历史场景。控制方保管答案，执行方只获得任务与资源。
- 新增实际产物读取工具，拒绝越界、符号链接、超限文件；采集后核对输入摘要，缺失执行保持未评估。
- 文本模型评测、宿主真实执行导入、人工教学数据分别说明，不声称已具备任意特定宿主的自动执行接口。本轮不变更八维评分算法。
- 一个宿主 Agent 实际完成 5 个任务，1 个受用量限制未完成；两个完整对照场景未显示增益，不生成总体改善差值。保存原始回答、产物和未覆盖的定性观察。
- 新增 4 项采集与任务包测试，累计 44 项测试、类型检查及构建通过。

## 2026-09-17 — Skill 入口调优（工具 0.2.0）

- 将当前机器的 Skill 入口纳入项目维护；调整能力承诺、批量归并和格式通用的报告生成验收，保留默认离线和按需语义调用。
- 入口与报告统一使用“八维证据审查”；质量分仍未知，本轮不引入新的评分模型。
- 明确 `--fail-on none` 不隐藏问题、报告生成不代表语义成功；扩展 CLI 验证，网络响应使用测试替身。
- CLI 帮助补全比较不可比的退出码含义；评分、案例和行为评测协议保持不变。

## 0.2.0 — 2026-09-16

- 修复虚高评分根因：不再将缺陷列表为空等同于质量满分，也不再将模型请求完成当成质量达标。
- 八维 score 统一保持未知，新增问题负担、问题数量和各维度缺失证据；保留逐项原文依据。
- Markdown / HTML 移除默认满分、94 分和进度条，突出证据不足及验证缺口。
- 审查报告升级为 1.1，新增 scoringPolicy。历史 1.0 报告可读，但旧口径分值不能用于质量改善结论。
- 新增 6 项回归测试，累计 39 项通过。基线案例与行为评测协议未变。

这次修正避免不成立的质量结论；经真实样本校准的质量评分仍未完成。

## 0.1.0 — 2026-09-16

- 从空仓库建立模块化 TypeScript 核心库、CLI、依赖锁与 CI。
- 实现 YAML / AST 解析、结构模型、源码证据、静态规则和文件限制。
- 接入可注入 LLM、语义输出核验、六类补丁分析、安全边界保留。
- 提供八维证据分、重构计划、文本 Diff、JSON / Markdown / HTML 和报告对比。
- 实现三类案例、审核状态、确定性 / rubric Grader、多轮三组评测、历史输出导入。
- 提供实际可运行脚本和零值历史错误测试，33 项测试通过。
- 验收修复：排除执行上下文中的测试答案、隔离运行器可变对象；不完整对照的总体差值标记为不可比。

外部 LLM 尚未实连；内置 Runner 为文本评测，不替代真实 Agent 工具执行。HTML 截图验收受本地文件 URL 策略限制。
