# Skill Doctor 工程计划

初始检查：2026-09-16，目录仅有空 Git 仓库，无旧实现。采用 TypeScript 核心库 + CLI + 离线 HTML 报告。

## 阶段 1：确定性核心
- [x] 带源码位置的 Skill 模型、YAML / Markdown AST、资源清单与内容摘要
- [x] 长度、Token 估算、标题、规则与分支统计、重复、引用与目录检查
- [x] 可配置阈值、解析边界测试、静态检查回归测试
- [x] 更新 README 并记录验证结果：类型检查 + 8 个测试通过

## 阶段 2：语义与补丁债
- [x] 可注入 LLM Provider、结构化输出校验、证据位置核验
- [x] 补丁候选、六类语义分类、根因假设、原则提炼、安全边界保留
- [x] 重构归属建议与八维证据评分；缺失评测显式标识
- [x] 故障 / 幻觉证据测试，更新 README：类型检查 + 累计 16 个测试通过

## 阶段 3：回归闭环
- [x] 三类案例生成、来源追踪、草稿 / 已审核状态
- [x] 确定性 Grader、可注入 rubric judge、失败和未评估区分
- [x] With-Skill / Without-Skill / 旧 Skill 对照，多轮统计与追踪
- [x] 历史结果导入、基线比较、真实失败案例测试，更新 README：累计 24 个测试通过

## 阶段 4：交付与维护
- [x] CLI、JSON / Markdown / HTML 报告与版本差异
- [x] 可运行样例、配置、架构 / 规则 / 协议文档
- [x] 跨平台 CI 定义、Windows 完整测试、构建和编译后 CLI 端到端验证
- [x] 更新 README、CHANGELOG 和后续路线图

完成条件：六个模块可通过公共 API / CLI 使用；所有扣分可定位证据；无 LLM 的检查可离线运行；生成案例不会以空断言自动通过；报告可以比较；默认不修改被审查 Skill。

## 验收记录与后续工作

本轮已通过 33 项测试、严格类型检查、构建，以及编译后 audit / eval / compare / diff 命令。确认自定义运行器不接收 tests 答案，各轮可变对象隔离，不完整评测不输出总体改善差值。

- [ ] 在用户选定的真实模型 endpoint 上校准语义审查和案例生成；当前仅有协议注入测试。
- [ ] 在远程 CI 实际执行 Linux / Windows、Node 22.18 / 24 矩阵；本轮本机为 Windows / Node 24.19。
- [ ] 完成 HTML 浏览器视觉验收；当前内置浏览器的文件 URL 策略阻止打开本地报告。
- [ ] 收集真实 Skill 与人工标注，校准阈值、误报率和评分权重。
- [ ] 增加实际 WorkBuddy / Agent TaskRunner，验证工具执行、产物与渐进式加载。

这些后续项为真实环境验证和扩展，不以模拟结果充当已完成。

## 0.2.0 评分纠正

- [x] 复现“缺少案例扣 6 分，其余维度接近满分”，先添加会失败的回归测试。
- [x] 撤销无正向证据的质量分；保留问题权重、来源与逐维验证缺口。
- [x] 同步 JSON / Markdown / HTML，新旧评分口径不计算质量改善。
- [x] 39 项测试、类型检查与构建通过，使用实际 skill-doctor 技能重跑前后报告。
- [x] 将验证后的修复同步到 WorkBuddy 正在使用的 A 盘部署副本。
- [ ] 完成真实样本标注、正向达标标准与行为验证后，再提供有依据的质量分。

用户已接入 WorkBuddy；CLI 可调用、语义审查已完成、行为回归已执行是三个不同状态。不能以已接入代替后两项验证。

## 2026-09-17：Skill 入口调优

- [x] 将已安装入口纳入 skills/skill-doctor 维护，只调整能力承诺、报告解读与验收。
- [x] 保留默认离线；按严重度与问题类型归并，说明本轮实际检查范围。
- [x] 验证报告生成与审查成功的区别，运行工程验证、Skill 格式校验和编译后命令。
- [x] 更新文档，备份后同步 WorkBuddy 工具与已安装入口，核对部署文件摘要。

验证结果：40 项测试通过，类型检查与构建通过；编译后 JSON / Markdown / HTML 三种报告生成正常。Skill 自带 Python 校验器缺少 PyYAML，改用项目 YAML 解析器检查 frontmatter、名称、描述、允许字段与未完成标记，并完成静态审查。没有运行真实 LLM 或 WorkBuddy 行为评测；入口的 tests.missing 仍如实保留。

## 宿主 Agent 真实对照验证

- [x] 固定三个历史问题场景，隔离任务输入与评分答案，保留原始文件和产物。
- [x] 宿主实际执行 5 个任务并核对产物与输入完整性；第 6 个受用量限制未完成，单列待补。
- [x] 导入现有 Grader 生成逐项结果，记录宿主、模型、任务标识与局限。
- [x] 更新评测文档与 README，44 项测试、类型检查及构建通过。
- [ ] 宿主额度恢复后补测 mixed-policy-comparison 的 with_skill 组，再扩充案例与重复次数。
- [ ] 恢复有明确定义与证据的百分制审查分；运行测试作为独立结果，不把未执行测试直接计为设计缺陷。

## 0.3.0 设计评分与完整审查

- [x] 八维四项标准、百分制、达标/扣分证据与静态暂评标识
- [x] 排除 Python 缓存误扣，识别根目录自检与扫描脚本候选
- [x] 宿主语义任务包、指纹与逐字证据校验、评分和报告整合
- [x] 更新历史报告比较、技能入口、README 与规则文档
- [x] 完成类型检查、45 项测试、构建；编译后 prepare-review、语义导入 audit（三种格式）及 compare 验证通过
- [x] 同步 WorkBuddy 部署与安装入口；203 个文件哈希核验，部署后语义导入八维评分与本地一致

## 0.3.1 完成门禁与模型授权

- [x] 完整体检默认执行语义审查，不逐次询问是否需要
- [x] 已配置/已选择服务视为已有授权；未知新远程地址首次发送前说明
- [x] 普通 audit 的静态结果退出 3，显式 --static 才按静态范围完成
- [x] 运行类型检查、46 项测试、构建和编译后 CLI 三种模式验收
- [x] 同步 WorkBuddy 部署与安装入口；203 个文件哈希一致，部署后 0.3.1 三种模式与八维完成状态核验通过
